export function Limitations() {
  return (
    <p className="footer-note">
      Simplified spatial prototype. Each hex is a lobule with a 3-zone gradient inspired by
      hepatic zonation and APAP-type centrilobular injury. Parameters are illustrative, not
      fitted to a clinical dataset. For preclinical candidate triage illustration only — not
      diagnostic, not a dosing device, not a trial replacement.
    </p>
  )
}
